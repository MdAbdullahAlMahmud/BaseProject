<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/colorAccent" />             <!-- Background color of the button -->
    <corners android:radius="30dp" />              <!-- Radius to make the button rounded -->
    <padding
        android:left="16dp"
        android:top="8dp"
        android:right="16dp"
        android:bottom="8dp" />                     <!-- Padding inside the button -->
</shape>